trigger:
- develop

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: InitialCheck
  displayName: 'Initial Pipeline Check'
  jobs:
  - job: Check
    displayName: 'Run Basic Check'
    steps:
    - script: echo "Pipeline is running successfully!"
      displayName: 'Echo Message 1'
